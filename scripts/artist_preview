#!/usr/bin/env python
# -*- coding: utf-8 -*-

"""
ArtistPreview 命令行入口脚本
"""

import sys
import os
import logging

# 添加项目根目录到Python路径
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

from src.artist_preview import main

if __name__ == "__main__":
    try:
        sys.exit(main())
    except Exception as e:
        logging.error(f"执行过程中发生错误: {e}", exc_info=True)
        sys.exit(1)